# Branching

- `main`: stable/playable releases only.
- `develop`: integration branch for tested feature work.
- `feature/*`: focused changes merged into `develop` before promotion to `main`.

Keep upstream-derived changes and custom-server features in small, reviewable units so upstream pin updates remain manageable.
