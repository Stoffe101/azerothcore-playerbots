# repo-pins.txt — pin upstream repos to known-good commits.
#
# Each non-comment line "<repo-dir> <commit>" freezes that repo at <commit> instead of tracking
# its branch tip. Both setup.sh and update.sh read this file (via pin_for/apply_pin), so a
# pinned repo stays put across updates. Use this to hold a stable upstream when the latest HEAD
# is broken; delete a line (or comment it with #) to resume normal branch-tip updates.
#
# Blank lines and lines starting with # are ignored. <repo-dir> is the directory basename:
#     azerothcore-wotlk   = the core playerbots fork (AC_DIR)
#     mod-playerbots      = the bot engine (azerothcore-wotlk/modules/mod-playerbots)
#     mod-dungeon-clear   = autonomous dungeon-driving extension for playerbots
#     mod-aoe-loot, ...   = any other module under azerothcore-wotlk/modules/
#
# --- ACTIVE PINS ---
# 2026-09-14 AI refresh: bumped the paired mod-playerbots + Playerbot-core pins to the
# current upstream integration tips. These tips are direct descendants of our previous pins
# (old pins were the first parent of the upstream merge), bringing in the September dungeon,
# class-AI, RPG/world-behaviour, guild and Ulduar/core fixes while preserving deterministic
# reproducibility. The local ./update.sh build remains the final compatibility gate for our
# overlay patches and EraTalents patch stack.
azerothcore-wotlk 06234df3d5ab26c93f4f1f06f3edb828b73ecd3c
mod-playerbots b6696bdbd3740e575598d167d69f39f68cc0b907
mod-individual-progression 977e2005bacf97f35e506eb27b8af6b2ea1136af
# mod-multibot-bridge: its old pin (5e5ff75) only existed because the bridge's Self-Bot code needed
# IsSelfBot(), which mod-playerbots ba46fcd lacked; b949b50 defines it, so the bridge now rides its
# tip. Upstream REWROTE the bridge's main as an orphan history (no shared ancestry with 5e5ff75) —
# patch 0022 was re-verified against it. Pinned like the others so an update can't outrun 0022.
mod-multibot-bridge 759c100d5e508b9af28531a2642ded70619828ba
# Autonomous dungeon driving. Pinned to the 2026-09-14 Oculus/ToC/CoS-capable revision so the
# server gets reproducible navmesh routing, scripted-event handling, recovery and heroic tuning.
# We deliberately keep this as a normal upstream git module instead of vendoring a fork.
mod-dungeon-clear 29c53fd10f99c1f978fba6cecd326bde8ce170d9
# era talents module (own repo: https://github.com/lathcf/azerothcore-mod-era-talents). The sha is a commit on the PUBLIC
# GitHub history (an orphan branch; the private Gitea history has different shas) — production clones from
# GitHub, so a private sha would never fetch. Bump after each public sync, after verifying a build.
mod-era-talents 3a0d9776c6837d8a5e5038c23c01c4fc388d90f8
