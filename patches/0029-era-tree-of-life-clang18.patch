diff --git a/modules/mod-era-talents/src/EraTalentProcScripts.cpp b/modules/mod-era-talents/src/EraTalentProcScripts.cpp
--- a/modules/mod-era-talents/src/EraTalentProcScripts.cpp
+++ b/modules/mod-era-talents/src/EraTalentProcScripts.cpp
@@ -2843,2 +2843,2 @@ class era_dru_tree_of_life : public AuraScript
-        amount = int32(caster->GetStat(STAT_SPIRIT) * ERA_TOL_SPIRIT_PCT / 100.0f);
+        amount = int32(caster->GetStat(STAT_SPIRIT) * static_cast<float>(ERA_TOL_SPIRIT_PCT) / 100.0f);
         // The core never recalculates SPELL_AURA_MOD_HEALING on a stat change, so pin it: the amount
