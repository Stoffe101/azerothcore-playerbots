[worldserver]

########################################
# mod-playerbot-chatter configuration
########################################

# PlayerbotChatter.Enable
#   Master switch. Default 0. setup.sh sets this from .env CHATTER_ENABLE.
PlayerbotChatter.Enable = 0

# PlayerbotChatter.Debug
#   Extra logging of classification + Ollama requests. Default 0.
PlayerbotChatter.Debug = 0

# PlayerbotChatter.Url
#   Full Ollama /api/generate URL. Default http://localhost:11434/api/generate
PlayerbotChatter.Url = http://localhost:11434/api/generate

# PlayerbotChatter.Model    Default gemma4:e4b
PlayerbotChatter.Model = gemma4:e4b

# PlayerbotChatter.Think
#   0 -> send "think": false (gemma4:e4b is a reasoning model; keep OFF). Default 0.
PlayerbotChatter.Think = 0

# PlayerbotChatter.StyleExamplesFile
#   Path (as seen by the worldserver process) to a plain-text file of few-shot STYLE lines
#   fed to the model as "match this tone/length, not the content" anchors, on both the
#   ambient and reactive paths. One example per line; blank lines and '#' comments skipped.
#   Live-editable: edit the file and restart ac-worldserver; no rebuild. If empty or the
#   file can't be read, the module falls back to its built-in list.
PlayerbotChatter.StyleExamplesFile = /azerothcore/modules/mod-playerbot-chatter/data/style-examples.txt

# PlayerbotChatter.SystemPrompt   The character charter sent as the Ollama "system" field.
PlayerbotChatter.SystemPrompt = "You're a real person playing WoW: Wrath of the Lich King (3.3.5a, level cap 80), hanging out in-game and chatting with other players. Type like a normal gamer in chat: short, relaxed, light slang (lol, gg, lfg, lfm, ding, brb, gz, ty, wtb, wts, pst). You're here to hang out, not to narrate your play session — talk about the game, the server, other players, your class, hot takes and gripes, the community, or whatever is on your mind, and the occasional totally off-topic real-life aside (tired, it is late, need coffee) is fine too. Don't invent fake WoW content or activities that do not exist. Each message quietly tells you your current level, as background only: never announce or tack your level onto what you say (no lvl-42 or level-42 tags), and keep any specific game references to things you would actually know by that level — if you are low or mid level you have NOT been to Northrend, run heroics, or raided (Naxxramas/Ulduar/ToC/ICC) and you never talk as if you have; only level-80 characters talk about heroics, raids, dailies, rep grinds, or endgame PvP. You're the person behind the keyboard, not the in-game character or an NPC — no fantasy roleplay voice. Vary how you start; never open with the word anyone. You've got a real personality: easygoing but opinionated, with a sense of humor, not a chipper customer-service bot. Be cranky, dry, and sarcastic when it fits — gripe about bad RNG, repair bills, wipes, grindy rep and dailies, class balance, the dungeon finder, blizzard, other servers, or the community; rib other players and share blunt opinions. Keep a floor though: no slurs, no real-world politics, and never genuinely hostile toward or targeting the person you are talking to — cranky and sarcastic is fine, cruel is not. Still help if someone actually asks (a little sarcasm about it is fine). Vary how the humor lands so you do not sound one-note. Never say you're an AI, bot, or game master. No markdown, emojis, asterisk-actions, or quotation marks."

# PlayerbotChatter.ReplyMaxLen       Max reply length in characters. Default 200.
PlayerbotChatter.ReplyMaxLen = 200
# PlayerbotChatter.MaxConcurrent     Max simultaneous Ollama requests (GPU gate). Default 4.
PlayerbotChatter.MaxConcurrent = 4

# Fan-out
PlayerbotChatter.SayRange = 40
PlayerbotChatter.SayMaxBots = 2
PlayerbotChatter.SayChance = 35
PlayerbotChatter.GroupMaxBots = 2
PlayerbotChatter.GroupChance = 50
# PlayerbotChatter.GroupGuaranteeOne
#   1 -> in party/raid chat, if the per-bot GroupChance roll picks nobody (and you didn't
#   name a bot), force one random party bot to reply. Named bots still always answer.
PlayerbotChatter.GroupGuaranteeOne = 1
PlayerbotChatter.WhisperChance = 100
PlayerbotChatter.HistoryLen = 10

# PlayerbotChatter.CommandKeywords
#   Comma-separated. Messages beginning with one of these are left for Playerbots commands.
PlayerbotChatter.CommandKeywords = "follow,stay,flee,grind,attack,tank attack,do attack,accept,talk,reset,runaway,summon,q,c,u,e,ue,t,nt,s,b,r,rep,items,inv,pvp stats,add all loot,move from group,enter vehicle,leave vehicle,buy,sell,trade,cast,co,nc,rti,los,ll"

########################################
# Persistent guild autonomy
########################################

# Runs real Playerbot actions for player-led guilds: crafting, gathering/travel,
# useful trades, mail, vendor activity, guild-bank interaction, recruitment and
# spontaneous dungeon/raid group formation. It does not mint passive profession gold.
PlayerbotChatter.GuildAutonomyEnable = 1
PlayerbotChatter.GuildAutonomyTickSeconds = 60
# Random delay between larger autonomous guild activities. Relationship/group-history
# maintenance still runs on the normal tick while the larger activity timer is cooling down.
PlayerbotChatter.GuildAutonomyMinMinutes = 5
PlayerbotChatter.GuildAutonomyMaxMinutes = 12

########################################
# Grounded raid leader
########################################

# Pre-pull briefs + retry advice only. Mid-fight mechanic callouts are intentionally left
# to WeakAuras. Unknown/unvalidated bosses stay silent rather than receiving invented tactics.
PlayerbotChatter.RaidLeaderEnable = 1
PlayerbotChatter.RaidLeaderBriefOnPull = 1
PlayerbotChatter.RaidLeaderRetryAdvice = 1

# When enabled and PlayerbotChatter.Enable=1, Ollama may rewrite the server-validated facts
# into one natural raid-chat line. The deterministic validated line is always the fallback.
# Set to 0 for strictly deterministic wording with no model involvement.
PlayerbotChatter.RaidLeaderUseOllama = 1

########################################
# Ambient (self-initiated) chatter - Phase 2
########################################

# PlayerbotChatter.AmbientEnable
#   Master switch for self-initiated chatter. Default 0. setup.sh ties this to
#   .env CHATTER_AMBIENT_ENABLE (which defaults to CHATTER_ENABLE).
PlayerbotChatter.AmbientEnable = 0

# Per-surface toggles. Only contexts with a real player present ever speak.
PlayerbotChatter.AmbientGeneral = 1
PlayerbotChatter.AmbientGroup = 1
PlayerbotChatter.AmbientGuild = 1

# Two-speed cadence (seconds). Seed = cold-channel opening line; Follow = fast
# in-thread reply while a context is active.
PlayerbotChatter.AmbientSeedMin = 60
PlayerbotChatter.AmbientSeedMax = 90
PlayerbotChatter.AmbientFollowMin = 4
PlayerbotChatter.AmbientFollowMax = 9
PlayerbotChatter.AmbientActiveWindow = 75

# Loop brakes. Bots may chat with each other, bounded by these.
PlayerbotChatter.AmbientBotStreakMax = 4
PlayerbotChatter.AmbientCooldown = 75
PlayerbotChatter.AmbientPerBotCooldown = 120

# Global safety ceiling (server-wide ambient messages per minute).
PlayerbotChatter.AmbientMaxPerMin = 25

# Per-context rolling buffer depth (recent lines fed to react mode).
PlayerbotChatter.AmbientBufferLen = 8

# Content-mode weights.
PlayerbotChatter.AmbientWeightBanter = 40
PlayerbotChatter.AmbientWeightGeneric = 10
PlayerbotChatter.AmbientWeightReact = 35
PlayerbotChatter.AmbientWeightFlavor = 3
PlayerbotChatter.AmbientWeightEvent = 8

########################################
# Lore sidecar - factual Q&A (Tier-2)
########################################

# PlayerbotChatter.LoreEnable
#   When 1, likely factual whispered questions are routed through the local lore sidecar.
PlayerbotChatter.LoreEnable = 0

# PlayerbotChatter.LoreUrl   Full /ask URL of the sidecar.
PlayerbotChatter.LoreUrl = http://ac-lore:8091/ask

# PlayerbotChatter.LoreTimeout
#   Seconds to wait for the sidecar before falling back to a normal reply.
PlayerbotChatter.LoreTimeout = 60
