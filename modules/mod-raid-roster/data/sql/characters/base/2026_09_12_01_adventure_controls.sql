CREATE TABLE IF NOT EXISTS `mod_adventure_controls` (
  `guid` INT UNSIGNED NOT NULL,
  `xp_percent` SMALLINT UNSIGNED NOT NULL DEFAULT 100,
  `gold_percent` SMALLINT UNSIGNED NOT NULL DEFAULT 100,
  `rep_percent` SMALLINT UNSIGNED NOT NULL DEFAULT 100,
  PRIMARY KEY (`guid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
